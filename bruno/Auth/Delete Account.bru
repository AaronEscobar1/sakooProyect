meta {
  name: Delete Account
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/auth/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "test@sakoo.com",
    "password": "Password123!"
  }
}
